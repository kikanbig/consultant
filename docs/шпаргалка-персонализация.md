# 🚀 Шпаргалка: Персонализация по колонкам

## ⚡ Быстрый старт (3 шага)

### 1️⃣ Узнать ID колонок

**Самый простой способ:**
1. Запустите навык на колонке
2. Скажите: **"Мой айди"** или **"Покажи ID"**
3. Алиса скажет Client ID - скопируйте его!

**Альтернативный способ (через логи):**
```javascript
// В src/config/deviceContent.js включите:
settings: {
  logDeviceInfo: true
}
```

Запустите навык на каждой колонке → смотрите логи Railway → копируйте Client ID

📚 **Подробная инструкция**: `docs/как-узнать-id-колонки.md`

### 2️⃣ Добавить маппинг

```javascript
// В src/config/deviceContent.js:
deviceMapping: {
  'ABC123XYZ456': 'kitchen',    // ← ваш реальный Client ID
  'DEF789GHI012': 'bedroom',
  'JKL345MNO678': 'hall',
}
```

### 3️⃣ Настроить контент

```javascript
locationContent: {
  kitchen: {
    name: 'Кухня',
    welcome: "Добро пожаловать в кухонную зону!",
    mainCategories: ['посуда', 'техника'],
    promotions: "Скидка 20% на посуду!",
    recommendedShelves: [1, 2, 3],
    defaultButtons: [
      { title: "Посуда", hide: true },
      { title: "Акции", hide: true }
    ]
  }
}
```

## 📋 Что можно персонализировать

✅ **Приветствие** - разное для каждой зоны  
✅ **Акции** - свои акции в каждой зоне  
✅ **Категории** - релевантные товары  
✅ **Кнопки** - удобная навигация  
✅ **Стеллажи** - только нужные стеллажи  

## 🎯 Примеры использования

### Сценарий 1: Зоны магазина
```
Колонка 1 (кухня)   → посуда, техника
Колонка 2 (спальня) → кровати, матрасы
Колонка 3 (гостиная) → диваны, кресла
```

### Сценарий 2: Разные магазины
```
Колонка в магазине А → свои акции
Колонка в магазине Б → другие акции
```

## 🔧 Файлы для редактирования

- **`src/config/deviceContent.js`** - вся настройка здесь
- **`src/utils/deviceManager.js`** - не трогать (логика)
- **`src/handlers/mainHandler.js`** - не трогать (интеграция)

## 🧪 Тестирование

### Локально (curl):
```bash
curl -X POST http://localhost:3000/webhook \
  -H "Content-Type: application/json" \
  -d '{
    "request": {"command": "привет"},
    "session": {
      "new": true,
      "user": {"user_id": "test_user_kitchen"}
    },
    "meta": {
      "client_id": "ABC123XYZ456"
    },
    "version": "1.0"
  }'
```

### На реальных колонках:
1. Запустите навык
2. Скажите "Привет"
3. Проверьте, что контент соответствует зоне

## ⚙️ Настройки

```javascript
settings: {
  enableDevicePersonalization: true,  // Включить/выключить
  logDeviceInfo: true,                // Логи (для отладки)
  showLocationInWelcome: false        // Показывать название зоны
}
```

## 🆘 Частые проблемы

**Не определяется Client ID?**
→ Проверьте логи, используйте user_id как запасной вариант

**Контент не меняется?**
→ Проверьте Client ID в маппинге, перезапустите навык

**Все колонки показывают одно?**
→ Проверьте, что `enableDevicePersonalization: true`

## 📝 Шаблон новой локации

```javascript
new_location: {
  name: 'Название зоны',
  welcome: "Текст приветствия",
  mainCategories: ['категория1', 'категория2'],
  promotions: "Информация об акциях",
  recommendedShelves: [1, 2, 3],
  defaultButtons: [
    { title: "Кнопка 1", hide: true },
    { title: "Кнопка 2", hide: true }
  ]
}
```

## ✅ Чек-лист

- [ ] Включил `logDeviceInfo: true`
- [ ] Запустил навык на всех колонках
- [ ] Скопировал Client ID из логов
- [ ] Добавил маппинг устройств
- [ ] Настроил контент для каждой зоны
- [ ] Протестировал на каждой колонке
- [ ] Задеплоил на Railway

---

**Полная документация**: `docs/персонализация-по-устройствам.md`


